# 🎯 快速測試指南

## 立即測試食材辨識功能

### 1️⃣ 啟動應用
```bash
cd frontend
flutter run
```

### 2️⃣ 測試步驟
1. **登入 APP**
2. **點擊底部導航列的「拍照辨識」圖示** （第二個 TAB，相機圖示）
3. **進入食材辨識頁面**

### 3️⃣ 測試功能
- **點擊「拍照」按鈕** → 開啟相機拍攝食材
- **點擊「相簿」按鈕** → 從相簿選擇食材照片

### 4️⃣ 查看結果
- 辨識結果會顯示在圖片下方
- 包含每種食材的名稱和數量
- 例如：「辨識到：2 個 高麗菜, 1 個 紅蘿蔔」

## 📸 測試建議

### 使用測試圖片
你可以使用以下方式測試：
1. 從網路下載蔬菜圖片
2. 拍攝真實的蔬菜
3. 使用 `cam/` 資料夾中的範例圖片（如果有）

### 測試案例
- ✅ 單一食材（如一顆高麗菜）
- ✅ 多種食材（如高麗菜 + 紅蘿蔔 + 番茄）
- ✅ 相同食材多個（如 3 個番茄）
- ✅ 不同光線條件
- ✅ 不同拍攝角度

## 🐛 如遇問題

### Android 權限問題
如果無法使用相機或相簿，需要在 `android/app/src/main/AndroidManifest.xml` 中添加權限：
```xml
<uses-permission android:name="android.permission.CAMERA"/>
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
```

### iOS 權限問題
如果無法使用相機或相簿，需要在 `ios/Runner/Info.plist` 中添加描述：
```xml
<key>NSCameraUsageDescription</key>
<string>需要使用相機來拍攝食材照片</string>
<key>NSPhotoLibraryUsageDescription</key>
<string>需要存取相簿來選擇食材照片</string>
```

## ✅ 預期行為

### 成功案例
- 辨識到食材：顯示綠色結果框，列出食材和數量
- 處理中：顯示載入動畫和「辨識中，請稍候...」
- 圖片預覽：選擇的照片正確顯示

### 錯誤處理
- 未辨識到食材：顯示「未辨識到任何食材，請重新拍攝」
- 模型載入失敗：頁面頂部顯示紅色錯誤訊息
- 圖片選擇失敗：顯示錯誤提示

## 🎉 測試完成檢查清單

- [ ] 可以正常進入拍照辨識頁面
- [ ] 相機功能可以使用
- [ ] 相簿選擇功能可以使用
- [ ] 圖片可以正確顯示預覽
- [ ] 辨識功能可以正常運作
- [ ] 辨識結果正確顯示（中文名稱和數量）
- [ ] 可以多次拍照/選擇照片
- [ ] 沒有崩潰或錯誤

---

**準備好了嗎？開始測試吧！** 🚀
