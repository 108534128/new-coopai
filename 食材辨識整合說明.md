# 食材辨識功能整合說明

## ✅ 已完成的整合步驟

### 1. 檔案結構
```
frontend/
├── lib/
│   ├── screens/
│   │   ├── camera_screen.dart         ← 新增：拍照辨識頁面
│   │   └── home_screen.dart           ← 已更新：整合 camera_screen
│   └── services/
│       └── food_detection_service.dart ← 新增：ONNX 模型推理服務
├── assets/
│   ├── models/
│   │   └── best.onnx                  ← 已複製：YOLO 模型檔案
│   └── data/
│       ├── classes.json                ← 已複製：26 種食材類別
│       ├── chinese_map.json            ← 已複製：英中對照表
│       └── label_fixes.json            ← 已複製：標籤修正規則
└── pubspec.yaml                        ← 已更新：新增依賴
```

### 2. 功能說明

#### `camera_screen.dart`
- 完整的拍照/選擇照片介面
- 圖片預覽功能
- 即時顯示辨識結果
- 顯示每種食材的數量統計
- 使用提示說明

#### `food_detection_service.dart`
- ONNX Runtime 模型載入
- 圖片預處理（調整為 640x640，RGB 格式）
- YOLOv8 模型推理
- 非極大值抑制 (NMS)
- 標籤修正功能
- 中文名稱轉換
- 食材數量統計

### 3. 已安裝的依賴
- `onnxruntime: ^1.4.1` - ONNX 模型推理
- `image: ^4.0.0` - 圖片處理
- `image_picker: ^1.0.4` - 拍照/選擇照片

### 4. 整合到 Home Screen
- 底部導航列第二個 TAB「拍照辨識」已連接到 `CameraScreen`
- 點擊即可進入食材辨識功能

## 📱 使用流程

1. **開啟 APP** → 登入後進入首頁
2. **點擊底部「拍照辨識」TAB**
3. **選擇操作**：
   - 點擊「拍照」→ 開啟相機拍攝食材
   - 點擊「相簿」→ 從相簿選擇照片
4. **自動辨識** → 顯示辨識結果和食材數量
5. **重新拍攝** → 可繼續拍照或選擇其他照片

## 🎯 辨識功能特色

- **26 種食材**：支援常見蔬菜辨識
- **離線運作**：模型內建在 APP，無需網路
- **中文顯示**：自動轉換為中文名稱
- **智能修正**：自動修正模型常見錯誤
- **數量統計**：自動計算每種食材的數量

## ⚠️ 使用注意事項

### 最佳拍攝建議
- 確保光線充足
- 食材盡量平鋪，避免重疊
- 相機與食材保持適當距離
- 避免複雜背景

### 權限需求
- **相機權限**：拍照功能需要
- **相簿權限**：選擇照片需要

## 🔧 下一步建議

### 可選功能增強
1. **推薦食譜整合**
   - 辨識完成後，根據食材推薦相關食譜
   - 添加「用這些食材做菜」按鈕

2. **歷史記錄**
   - 儲存辨識記錄
   - 查看過去的辨識結果

3. **批次辨識**
   - 支援多張照片連續辨識
   - 合併顯示所有食材

4. **進階統計**
   - 顯示信心度
   - 顯示辨識框位置

## 🚀 測試步驟

1. **安裝依賴**：
   ```bash
   cd frontend
   flutter pub get
   ```

2. **執行 APP**：
   ```bash
   flutter run
   ```

3. **測試流程**：
   - 點擊「拍照辨識」TAB
   - 測試拍照功能
   - 測試相簿選擇功能
   - 檢查辨識結果是否正確顯示

## 📞 問題排查

### 常見問題

1. **模型載入失敗**
   - 檢查 `assets/models/best.onnx` 是否存在
   - 確認 `pubspec.yaml` 中的 assets 配置正確

2. **JSON 檔案讀取失敗**
   - 檢查 `assets/data/` 下的 3 個 JSON 檔案
   - 確認檔案格式正確

3. **權限問題**
   - Android: 檢查 `AndroidManifest.xml` 相機和儲存權限
   - iOS: 檢查 `Info.plist` 相機和相簿權限描述

4. **辨識結果不準確**
   - 確保拍攝條件良好（光線、角度、距離）
   - 避免食材重疊或遮擋
   - 檢查是否在支援的 26 種食材範圍內

## 📋 支援的食材清單

番茄、青花菜、洋蔥、馬鈴薯、酪梨、豆豆、甜菜根、青椒、
抱子甘藍、高麗菜、紅蘿蔔、花椰菜、芹菜、玉米、小黃瓜、
茄子、大蒜、辣椒、豌豆、南瓜、蘿蔔、生菜、櫛瓜、西葫蘆

---

**整合完成！** 🎉 現在可以直接使用拍照辨識功能了。
